/** @param {NS} ns */
export async function main(ns) {

	/*
	let serverList = ns.getPurchasedServers;
	let serverAmount = serverList.length;

	let serverIndex = 0;
	let multiplier = 0;

	for (serverIndex; serverIndex < serverAmount; serverIndex++) {
		if (ns.getPurchasedServerUpgradeCost(serverList[serverIndex], 2 ** multiplier) <= ns.getServerMoneyAvailable("home")) {
			ns.upgradePurchasedServer(serverList[serverIndex], 2 ** multiplier);
		} else {
			multiplier++;
		}
	}
	*/
}